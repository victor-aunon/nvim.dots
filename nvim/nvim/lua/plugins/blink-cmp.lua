return {
  {
    "saghen/blink.cmp",
    -- The `opts` table is where you configure the plugin.
    opts = {
      -- This section controls the keymaps for the completion menu.
      keymap = {
        -- The "super-tab" preset configures Tab to accept the first suggestion
        -- and Shift-Tab to go backward.
        preset = "super-tab",
      },
    },
  },
}
